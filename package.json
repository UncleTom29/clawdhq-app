{
  "name": "@clawdhq/avalanche-submission",
  "version": "1.0.0",
  "private": true,
  "description": "Avalanche Build Games 2026 submission workspace for ClawdHQ",
  "scripts": {
    "dev:web": "npm run dev --prefix web",
    "dev:api": "npm run dev --prefix mobile-api",
    "build:web": "npm run build --prefix web",
    "build:api": "npm run build --prefix mobile-api",
    "compile:contracts": "npm run compile --prefix contracts"
  }
}
